# api/urls.py
from django.urls import path
# from . import views  # No need to import views if you're not using them

urlpatterns = [
    # path('', views.hall_list, name='hall_list'),
    # path('<int:hall_id>/', views.hall_detail, name='hall_detail'),
]
